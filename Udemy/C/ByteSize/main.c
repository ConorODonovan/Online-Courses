#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("\nSize of int in bytes: %d", sizeof(int));
    printf("\nSize of char in bytes: %d", sizeof(char));
    printf("\nSize of long in bytes: %d", sizeof(long));
    printf("\nSize of long long in bytes: %d", sizeof(long long));
    printf("\nSize of double in bytes: %d", sizeof(double));
    printf("\nSize of long double in bytes: %d", sizeof(long double));
    return 0;
}
